#include <sys/types.h>
#include <unistd.h>
#include <fcntl.h>

ssize_t readln(int fd, char *line, size_t size)
{

}

int main (int argc, char* argv[])
{
    int fd;
    if (argc== 2)
        fd= open (argv[1], )
}